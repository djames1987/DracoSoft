veracrypt command line

generate key file:

veracrypt -t --non-interactive --create-key key1 --hash=SHA-512 --size=1024

encryption type:
AES(TWOFISH)

hash typpe:
SHA-512

max password = 64

use keyfiles

usee pim

create new volume:
veracrypt -t --non-interactive -C ~/Documents/test --encryption=AES(TWOFISH) --hash=SHA-512 --pim=1024 --size=1000 -- --new-password=testingtihisijfkjs
